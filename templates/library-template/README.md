# Library Template

This folder was previously managed as a git submodule. It has been converted to a local development folder so you can edit templates directly here.

If you want to restore the submodule later, update `.submodules.config` and run `make setup-submodules-init`.

Contents:
- Add template files used for library scaffolding.
